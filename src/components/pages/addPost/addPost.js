import AddPostForm from "../../Features/AddPostForm/AddPostForm";

const AddPost = (props) => {
  return (
    <div>
      <h2>Add Post</h2>
      <AddPostForm />
    </div>
  );
};

export default AddPost;
